services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      # Override any application.properties values here, e.g.:
      # SPRING_PROFILES_ACTIVE: prod
      JAVA_OPTS: ""
